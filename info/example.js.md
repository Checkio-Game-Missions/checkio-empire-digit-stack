**Example:**

```javascript
golf(["PUSH 3", "POP", "POP", "PUSH 4", "PEEK", "PUSH 9", "PUSH 0", "PEEK", "POP", "PUSH 1", "PEEK"]) == 8
golf(["POP", "POP"]) == 0
golf(["PUSH 9", "PUSH 9", "POP"]) == 9
golf([]) == 0
```
